{
  "name": "Stocks and Bearers API",
  "description": "Sample app that shows how I implement JSON API applications based on test task",
  "env": {
    "RACK_ENV": {
      "required": "staging"
    },
    "RAILS_ENV": {
      "required": "staging"
    },
    "SECRET_KEY_BASE": {
      "generator": "secret"
    }
  },
  "addons": [
    "heroku-postgresql"
  ]
}
